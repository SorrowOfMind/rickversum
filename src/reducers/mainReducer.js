const state = {};

export default mainReducer = (state, action) => {
    switch(action.type){
        case 'LOAD_RICKDATA':
            return state;
        default:
            return state;
    }
}